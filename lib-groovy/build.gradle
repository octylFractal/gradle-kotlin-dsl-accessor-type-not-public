/*
 * This file was generated by the Gradle 'init' task.
 *
 * This project uses @Incubating APIs which are subject to change.
 */

plugins {
    id("my-plugin")
}

myPlugin {
    if (dependencies.toString() != "extension dependencies") {
        throw new IllegalStateException("dependencies are not extension dependencies, but ${dependencies}");
    }
    println("Got extension dependencies!")
}
